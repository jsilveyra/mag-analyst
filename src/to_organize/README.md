# to_organize

Files with **zero live callers in `app_old_exported.m`** as of the 2026-07-06
reorg. Kept (not deleted) until their intended use is clarified. If a file
here gains a real caller, move it into its proper tab folder (or `Common/`);
don't leave stale files parked here once their use is known.
